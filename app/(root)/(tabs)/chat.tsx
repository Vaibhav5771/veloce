import { Text, View } from "react-native";

const Chat = () => {
  return (
    <View className="flex-1 items-center justify-center bg-blue-100">
      <Text className="text-xl font-bold text-red-500">Chat</Text>
    </View>
  );
};

export default Chat;
