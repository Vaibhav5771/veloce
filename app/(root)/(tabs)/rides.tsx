import { Text, View } from "react-native";

const Rides = () => {
  return (
    <View className="flex-1 items-center justify-center bg-blue-100">
      <Text className="text-xl font-bold text-red-500">Rides</Text>
    </View>
  );
};

export default Rides;
